import type {NextConfig} from "next";

/** Next.js application configuration. */
const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  allowedDevOrigins: ["192.168.100.245"],
};

export default nextConfig;
